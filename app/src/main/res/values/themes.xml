<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.Grogu" parent="Theme.AppCompat">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Text colors -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/teal_200</item>

        <!-- Font family -->
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="fontFamily">@font/roboto_regular</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/black</item>

        <!-- Ensure dark action bar -->
        <item name="android:actionBarStyle">@style/DarkActionBar</item>
    </style>

    <style name="DarkActionBar" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">@color/black</item>
        <item name="android:titleTextStyle">@style/DarkActionBar.TitleText</item>
    </style>

    <style name="DarkActionBar.TitleText" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Custom Text Style -->
    <style name="AppTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
